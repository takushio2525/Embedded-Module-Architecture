% API仕様
\section{API仕様}

\subsection{IModuleインターフェース}

\begin{apibox}{IModule — モジュールインターフェース}
\textbf{定義ファイル:} \texttt{lib/ModuleCore/IModule.h}

全ハードウェアモジュールが実装すべき抽象インターフェース。
\texttt{SystemData}の前方宣言のみを使用し、プロジェクト固有のヘッダーに依存しない。
\end{apibox}

\begin{lstlisting}[style=cppstyle, caption={IModuleインターフェース定義}]
// IModule.h — モジュールインターフェース（プロジェクト非依存）
#pragma once

// SystemDataの前方宣言
// 各プロジェクトでSystemData構造体を定義すること
struct SystemData;

class IModule {
public:
    // 仮想デストラクタ
    virtual ~IModule() {}

    // モジュールの初期化
    // 戻り値: true=成功, false=失敗
    virtual bool init() = 0;

    // モジュールの更新（毎ループ呼び出される）
    // 引数: data - プロジェクト共有データへの参照
    virtual void update(SystemData& data) = 0;

    // モジュールの終了処理（リソース解放）
    // デフォルトは空実装。解放が必要なモジュールのみオーバーライド
    virtual void deinit() {}

    // 動的有効/無効化フラグ
    // false の場合、ループ内で update() がスキップされる
    bool enabled = true;
};
\end{lstlisting}

\begin{notebox}[前方宣言によるプロジェクト非依存性]
\texttt{IModule.h}は\texttt{SystemData}を前方宣言のみで参照しているため、
\texttt{SystemData.h}を\texttt{\#include}する必要がない。\\
\texttt{update()}の引数は参照型（\texttt{SystemData\&}）であるため、前方宣言だけで十分であり、
どのプロジェクトにもそのままコピーして流用できる。
\end{notebox}

\begin{cautionbox}[データアクセスルール]
\texttt{update()}内では、\textbf{自モジュールのサブ構造体のみ}にアクセスすること。
他モジュールのサブ構造体への読み書きは禁止とする。
モジュール間のデータ連携が必要な場合は、必ずロジックフェーズを経由する。
\end{cautionbox}

\begin{notebox}[\texttt{SystemData}名の標準化]
\texttt{IModule.h}は前方宣言で\texttt{SystemData}という型名に依存している。
この名前は本アーキテクチャの標準命名規則として固定されており、
すべてのプロジェクトで\texttt{SystemData}という名前の構造体を\texttt{include/SystemData.h}に定義する。
\end{notebox}

\subsubsection{init()}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
戻り値 & \texttt{bool} — \texttt{true}で初期化成功、\texttt{false}で失敗 \\
呼び出しタイミング & \texttt{setup()}内で1回のみ \\
責務 & ハードウェアの初期化、ピン設定、ModuleTimerの\texttt{setTime()}呼び出し等 \\
エラー時 & \texttt{false}を返す。呼び出し側でモジュールの無効化やログ出力を行う \\
\bottomrule
\end{tabularx}

\subsubsection{update(SystemData\& data)}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
引数 & \texttt{SystemData\&} — プロジェクト共有データへの参照（nullにならない） \\
戻り値 & \texttt{void} \\
呼び出しタイミング & \texttt{loop()}内で毎ループ呼び出される \\
責務 & センサー読み取り、出力制御、共有データの更新 \\
エラー時 & 共有データ内のサブ構造体にエラーフラグを設定 \\
\bottomrule
\end{tabularx}

\subsubsection{deinit()}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
戻り値 & \texttt{void} \\
呼び出しタイミング & スリープ突入前、モジュール停止時等 \\
責務 & ペリフェラルの解放、GPIO状態のリセット等のクリーンアップ処理 \\
デフォルト実装 & 空（何もしない）。リソース解放が必要なモジュールのみオーバーライドする \\
\bottomrule
\end{tabularx}

\subsubsection{enabled}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
型 & \texttt{bool} — デフォルト値\texttt{true} \\
用途 & 実行時にモジュールを動的に有効化・無効化する \\
動作 & \texttt{false}の場合、ループ内で\texttt{update()}がスキップされる \\
設定タイミング & \texttt{init()}失敗時の無効化、BLE等の外部通信からの切り替え、ロジックフェーズでの制御 \\
\bottomrule
\end{tabularx}

\subsubsection{使用例}

\begin{lstlisting}[style=cppstyle, caption={モジュール実装の例（ヘッダー）}]
// LedModule.h
#pragma once
#include <Arduino.h>
#include "IModule.h"

// モジュール固有のConfig構造体
struct LedConfig {
    uint8_t ledPin;  // LED出力ピン
};

// モジュール固有のデータ構造体（メンバにデフォルト値を明示）
struct LedData {
    bool state = false;
    uint8_t brightness = 0;
};

// SystemDataの前方宣言はIModule.h内で行われている

// IModuleを継承
class LedModule : public IModule {
private:
    LedConfig _config;  // Configをメンバとして保持
public:
    // コンストラクタでConfig構造体を受け取る
    LedModule(const LedConfig& config);
    bool init() override;
    void update(SystemData& data) override;
};
\end{lstlisting}

\begin{lstlisting}[style=cppstyle, caption={モジュール実装の例（実装ファイル）}]
// LedModule.cpp
#include "LedModule.h"
#include "SystemData.h"  // SystemDataの完全な定義を取得

LedModule::LedModule(const LedConfig& config) : _config(config) {}

bool LedModule::init() {
    pinMode(_config.ledPin, OUTPUT);  // Configのピンを使用
    Serial.println("[Led] init OK");
    return true;
}

// data はポインタではなく参照なので、ドット演算子でアクセス
void LedModule::update(SystemData& data) {
    digitalWrite(_config.ledPin, data.led.state);
}
\end{lstlisting}

% -------------------------------------------

\subsection{ModuleTimerクラス}

\begin{apibox}{ModuleTimer — ノンブロッキングタイマー}
\textbf{定義ファイル:} \texttt{lib/ModuleCore/ModuleTimer.h}

\texttt{millis()}ベースのシンプルなタイマークラス。
\texttt{delay()}を使用せずに周期実行・デバウンス・ワンショット・タイムアウト等を実現する。
インスタンスを複数作成することで、モジュール内で複数のタイマーを独立に管理できる。
IModuleと同様にプロジェクト非依存であり、そのまま流用可能。
\end{apibox}

\begin{lstlisting}[style=cppstyle, caption={ModuleTimerクラス定義}]
// ModuleTimer.h — プロジェクト非依存のコアヘッダー
#pragma once
#include <Arduino.h>

class ModuleTimer {
private:
    unsigned long _startTime;

public:
    // コンストラクタ
    ModuleTimer() : _startTime(0) {}

    // 基準時刻をセット
    // 引数なし: getNowTime()が0から開始
    // 引数あり: getNowTime()が指定値から開始
    void setTime(unsigned long offsetMs = 0) {
        _startTime = millis() - offsetMs;
    }

    // 基準時刻からの経過時間(ms)を取得
    unsigned long getNowTime() const {
        return millis() - _startTime;
    }
};
\end{lstlisting}

\subsubsection{setTime(unsigned long offsetMs = 0)}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
引数 & \texttt{offsetMs}（デフォルト: 0） — 初期オフセット値(ms) \\
戻り値 & \texttt{void} \\
動作 & 内部の基準時刻を\texttt{millis() - offsetMs}に設定する。
       \texttt{offsetMs = 0}の場合、直後の\texttt{getNowTime()}は0を返す。
       \texttt{offsetMs = 100}の場合、直後の\texttt{getNowTime()}は100を返す \\
用途 & タイマーの開始・リセット。\texttt{init()}内および周期リセット時に使用 \\
\bottomrule
\end{tabularx}

\subsubsection{getNowTime() const}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
引数 & なし \\
戻り値 & \texttt{unsigned long} — 最後の\texttt{setTime()}からの経過時間(ms) \\
動作 & \texttt{millis() - \_startTime}を返す \\
用途 & 経過時間の判定。条件分岐と組み合わせて周期実行・デバウンス等を実現 \\
\bottomrule
\end{tabularx}

\subsubsection{使用パターン}

\begin{lstlisting}[style=cppstyle, caption={周期実行パターン}]
ModuleTimer readTimer;

bool init() override {
    readTimer.setTime();
    return true;
}

void update(SystemData& data) override {
    if (readTimer.getNowTime() >= 1000) {
        readTimer.setTime();  // リセット
        data.temp.temperature = readSensor();
    }
}
\end{lstlisting}

\begin{lstlisting}[style=cppstyle, caption={デバウンスパターン}]
ModuleTimer debounce;

void update(SystemData& data) override {
    bool rawInput = digitalRead(BUTTON_PIN);

    if (rawInput != lastRawInput) {
        debounce.setTime();  // 入力変化でリセット
        lastRawInput = rawInput;
    }

    if (debounce.getNowTime() >= 50) {
        data.button.pressed = lastRawInput;  // 50ms安定で確定
    }
}
\end{lstlisting}

% -------------------------------------------

\subsection{ProjectConfigファイルとSystemData}

\begin{apibox}{ProjectConfig — Configインスタンスの集約}
\textbf{定義ファイル:} \texttt{include/ProjectConfig.h}（プロジェクト固有）

各モジュールのConfigインスタンス（ピンアサイン等）を集約した、プロジェクト固有の設定ファイル。
\texttt{main.cpp}のみがインクルードする。
\end{apibox}

\begin{apibox}{SystemData — モジュール間共有データ構造体}
\textbf{定義ファイル:} \texttt{include/SystemData.h}（プロジェクト固有）

各モジュールの\texttt{Data}構造体を集約した共有データ構造体。
\texttt{IModule}の\texttt{update()}メソッドの引数型として使用される。
モジュールの\texttt{.cpp}ファイルからインクルードされる。
\end{apibox}

\subsubsection{設計ルール}

\begin{enumerate}
    \item 各モジュールは、自身の\texttt{Config}構造体（型定義）と\texttt{Data}構造体（型定義）をモジュールのヘッダーファイル内に定義する
    \item \texttt{ProjectConfig.h}には、Configの\textbf{インスタンス}（実際のピン番号等の値）のみを定義する
    \item \texttt{SystemData}構造体は\texttt{SystemData.h}に分離し、プロジェクトで使用するモジュールのData構造体をメンバとして持つ
    \item モジュールの\texttt{.cpp}は\texttt{SystemData.h}をインクルードする（\texttt{ProjectConfig.h}はインクルードしない）
    \item モジュールは\texttt{update()}内で、\textbf{自身のData構造体のみ}にアクセスする（他モジュールのData構造体への読み書きは禁止）
\end{enumerate}

\begin{lstlisting}[style=cppstyle, caption={SystemData.h構成例}]
// SystemData.h — モジュール間共有データ構造体
#pragma once
#include "LedModule.h"
#include "TempSensorModule.h"
#include "CameraModule.h"

struct SystemData {
    LedData led;
    TempData temp;
    CameraData camera;
};
\end{lstlisting}

\begin{lstlisting}[style=cppstyle, caption={ProjectConfig.h構成例}]
// ProjectConfig.h — Configインスタンスのみ
#pragma once
#include "SystemData.h"

// ===== モジュールConfig（ピンアサイン・設定値） =====
const LedConfig LED_CONFIG = { .ledPin = 13 };
const TempSensorConfig TEMP_CONFIG = { .tempPin = 36 };
const CameraConfig CAMERA_CONFIG = { .sdaPin = 21, .sclPin = 22 };
\end{lstlisting}

\begin{notebox}[ファイル分離の理由]
\texttt{SystemData.h}を\texttt{ProjectConfig.h}から分離することで、
モジュールの\texttt{.cpp}ファイルは\texttt{ProjectConfig.h}を知る必要がなくなる。\\
\textbf{SystemData.h} — 各モジュールのData構造体を集約。モジュールの\texttt{.cpp}がインクルード。\\
\textbf{ProjectConfig.h} — Configインスタンス（ピン番号等の具体的な値）のみ。
\texttt{main.cpp}だけがインクルードすればよい。
回路設計担当者はこのファイルのConfig定義だけを参照すれば回路設計が可能。
\end{notebox}

% -------------------------------------------

\subsection{モジュールConfig構造体}

\begin{apibox}{モジュールConfig — ハードウェア設定構造体}
各モジュールが必要とするピンアサインや設定をまとめた構造体。
各モジュールのヘッダーファイル内で型を定義し、
\texttt{ProjectConfig.h}内で具体的な値を持つインスタンスを定義する。
\end{apibox}

\subsubsection{命名規則}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{要素} & \textbf{命名規則} & \textbf{例} \\
\midrule
Config構造体の型名 & \texttt{\{Module\}Config} & \texttt{LedConfig}, \texttt{CameraConfig} \\
Configインスタンス名 & \texttt{\{MODULE\}\_CONFIG}(大文字) & \texttt{LED\_CONFIG}, \texttt{CAMERA\_CONFIG} \\
Data構造体の型名 & \texttt{\{Module\}Data} & \texttt{LedData}, \texttt{CameraData} \\
\bottomrule
\end{tabularx}

\subsubsection{設計ルール}

\begin{enumerate}
    \item Config構造体の\textbf{型定義}は、各モジュールのヘッダーファイル内に記述する
    \item Config構造体の\textbf{インスタンス}（実際のピン番号等）は\texttt{ProjectConfig.h}に集約する
    \item モジュールのコンストラクタは\texttt{const \{Module\}Config\&}（const参照）でConfigを受け取る
    \item 受け取ったConfigは、メンバ変数\texttt{\_config}としてコピー保持する（Configは初期値として扱う）
    \item 実行時に設定値の変更が必要な場合は、\texttt{\_config}メンバを直接変更するか、セッターメソッドを用意する
    \item ピン番号はハードコードせず、必ず\texttt{\_config}経由でアクセスする
    \item 通信バスを使用するモジュールは、バスポインタをConfig構造体に含めず、コンストラクタの引数で受け取る（§通信バスパターン参照）
\end{enumerate}

\begin{lstlisting}[style=cppstyle, caption={Config構造体の定義と使用例}]
// TempSensorModule.h
#pragma once
#include "IModule.h"
#include "ModuleTimer.h"

// --- Config構造体（ピンアサイン・設定） ---
struct TempSensorConfig {
    uint8_t tempPin;          // アナログ入力ピン
    unsigned long interval;   // 読み取り間隔(ms)
};

// --- Data構造体（メンバにデフォルト値を明示） ---
struct TempData {
    float temperature = -999.0f;
    bool isValid = false;
};

class TempSensorModule : public IModule {
private:
    TempSensorConfig _config;
    ModuleTimer _readTimer;
public:
    // コンストラクタ: Config構造体をconst参照で受け取る
    TempSensorModule(const TempSensorConfig& config)
        : _config(config) {}

    bool init() override {
        pinMode(_config.tempPin, INPUT);
        _readTimer.setTime();
        return true;
    }

    void update(SystemData& data) override {
        if (_readTimer.getNowTime() >= _config.interval) {
            _readTimer.setTime();
            data.temp.temperature = analogRead(_config.tempPin);
            data.temp.isValid = true;
        }
    }
};
\end{lstlisting}

\begin{lstlisting}[style=cppstyle, caption={通信バスを使用するモジュールのConfig構造体とコンストラクタ例}]
// SpiSensorModule.h — Config構造体にはバスポインタを含めない
struct SpiSensorConfig {
    uint8_t csPin;            // チップセレクトピン
    uint32_t clockSpeed;      // SPIクロック速度(Hz)
    unsigned long interval;   // 読み取り間隔(ms)
};

class SpiSensorModule : public IModule {
private:
    SpiSensorConfig _config;
    SPIClass* _spi;  // バスポインタはメンバ変数として保持
public:
    // Config + バスポインタをコンストラクタで受け取る
    SpiSensorModule(const SpiSensorConfig& config, SPIClass* spi)
        : _config(config), _spi(spi) {}
};

// ProjectConfig.h — Configは純粋な設定値のみ
const SpiSensorConfig SPI_SENSOR_CONFIG = {
    .csPin = 5, .clockSpeed = 1000000, .interval = 100
};

// main.cpp — バスの紐付けはモジュール生成時に行う
SPIClass spi(HSPI);
SpiSensorModule spiModule(SPI_SENSOR_CONFIG, &spi);
\end{lstlisting}

% -------------------------------------------

\subsection{実装モジュール一覧}

本プロジェクトで実装しているモジュールの仕様を以下に示す。

% ---- TftModule ----
\needspace{5\baselineskip}
\subsubsection{TftModule — 2.8インチSPI TFT LCD + タッチ}

\begin{apibox}{TftModule}
\textbf{定義ファイル:} \texttt{lib/TftModule/TftModule.h/.cpp}\\
\textbf{ライブラリ:} TFT\_eSPI（ピン設定はplatformio.iniのbuild\_flagsで管理）\\
\textbf{ドライバ:} ST7789（240×320）/ XPT2046タッチコントローラ内蔵対応\\
\textbf{役割:} 出力（画面描画）+ 入力（タッチ読み込み）の複合モジュール
\end{apibox}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{構造体} & \textbf{メンバ} & \textbf{説明} \\
\midrule
\texttt{TftConfig} & \texttt{rotation} & 画面回転 0--3（1=横向き） \\
 & \texttt{blPin} & バックライトピン（-1で常時ON） \\
 & \texttt{updateIntervalMs} & 画面更新周期 [ms] \\
\midrule
\texttt{TftData} & \texttt{touchPressed} & タッチ押下中フラグ \\
 & \texttt{touchX / touchY} & タッチ座標（ピクセル） \\
 & \texttt{line1--line4} & 表示テキスト（char[48]、logicフェーズで設定） \\
\bottomrule
\end{tabularx}

\begin{notebox}[タッチと描画の実行タイミング]
\texttt{update()}内でタッチ読み込みと画面描画を順番に実行する。
タッチデータは\textbf{outputフェーズ}で更新され、次ループの\textbf{logicフェーズ}で参照される（1ループ分の遅延）。
SPI/タッチのピン番号は\texttt{platformio.ini}のbuild\_flagsで静的に設定する。
\end{notebox}

% ---- Mpu6500Module ----
\needspace{5\baselineskip}
\subsubsection{Mpu6500Module — MPU-6500 IMUセンサー}

\begin{apibox}{Mpu6500Module}
\textbf{定義ファイル:} \texttt{lib/Mpu6500Module/Mpu6500Module.h/.cpp}\\
\textbf{外部ライブラリ:} なし（Wireによる直接I2Cレジスタアクセス）\\
\textbf{役割:} 入力モジュール — 加速度・角速度・温度を読み取る
\end{apibox}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{構造体} & \textbf{メンバ} & \textbf{説明} \\
\midrule
\texttt{Mpu6500Config} & \texttt{address} & I2Cアドレス（AD0=LOW: 0x68 / HIGH: 0x69） \\
 & \texttt{sdaPin / sclPin} & I2CピンID \\
 & \texttt{sampleIntervalMs} & サンプリング周期 [ms] \\
\midrule
\texttt{Mpu6500Data} & \texttt{accelX/Y/Z} & 加速度 [g]（±2g, 16384 LSB/g） \\
 & \texttt{gyroX/Y/Z} & 角速度 [deg/s]（±250°/s, 131 LSB/°/s） \\
 & \texttt{temperature} & 温度 [°C]（デフォルト: -999.0f） \\
 & \texttt{isValid} & データ有効フラグ（デフォルト: false） \\
\bottomrule
\end{tabularx}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{レジスタ} & \textbf{説明} \\
\midrule
\texttt{0x75} WHO\_AM\_I & 0x70を期待（MPU-6500識別子） \\
\texttt{0x6B} PWR\_MGMT\_1 & 0x01: スリープ解除 + PLLクロック選択 \\
\texttt{0x19} SMPLRT\_DIV & 0x04: 200Hzサンプリング \\
\texttt{0x1A} CONFIG & 0x02: DLPFフィルタ（92Hz帯域幅） \\
\texttt{0x3B} ACCEL\_XOUT\_H & 加速度から角速度まで14バイト一括読み込み \\
\bottomrule
\end{tabularx}

% ---- CameraModule ----
\needspace{5\baselineskip}
\subsubsection{CameraModule — OV5640カメラ}

\begin{apibox}{CameraModule}
\textbf{定義ファイル:} \texttt{lib/CameraModule/CameraModule.h/.cpp}\\
\textbf{ライブラリ:} esp\_camera（ESP32 Arduino platform内蔵）\\
\textbf{役割:} 入力モジュール — OV5640からDVPバス経由でフレームを取得する
\end{apibox}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{構造体} & \textbf{メンバ} & \textbf{説明} \\
\midrule
\texttt{CameraConfig} & \texttt{pwdnPin / resetPin} & 電源/リセットピン（-1で未使用） \\
 & \texttt{xclkPin / siodPin / siocPin} & XCLKとSCCB（カメラ設定用I2C）ピン \\
 & \texttt{d0--d7Pin} & DVPデータバスピン \\
 & \texttt{vsyncPin / hrefPin / pclkPin} & 同期クロックピン \\
 & \texttt{xclkFreqHz} & XCLKクロック周波数（通常20MHz） \\
 & \texttt{frameSize} & フレームサイズ（framesize\_t の整数値） \\
 & \texttt{pixFormat} & ピクセルフォーマット（pixformat\_t の整数値） \\
 & \texttt{jpegQuality} & JPEG品質 0--63（低いほど高画質） \\
 & \texttt{fbCount} & フレームバッファ数（PSRAM使用時は2以上） \\
\midrule
\texttt{CameraData} & \texttt{isValid} & init成功フラグ \\
 & \texttt{frameReady} & 新フレーム取得済みフラグ \\
 & \texttt{width / height} & フレーム解像度 \\
 & \texttt{frameSize} & フレームバイトサイズ \\
\bottomrule
\end{tabularx}

\begin{cautionbox}[フレームバッファのライフサイクル]
フレームの生データ（\texttt{camera\_fb\_t*}）は\texttt{SystemData}に格納しない。
\texttt{CameraModule::getFrameBuffer()}でアクセスし、
使用後は\texttt{releaseFrame()}を呼ぶか、logicフェーズ末尾で\texttt{cameraModule.releaseFrame()}を呼ぶこと。
\textbf{次の\texttt{update()}呼び出しまでにリリースしなければ自動的に解放される。}\\
\texttt{CameraData}にはメタデータのみ格納し、\texttt{esp\_camera.h}のヘッダー依存をコア層に持ち込まない。
\end{cautionbox}

\begin{notebox}[PSRAM使用について]
\texttt{fbCount >= 2}かつPSRAMが検出された場合（\texttt{psramFound()} = true）、
フレームバッファは自動的にPSRAMに配置される。
OV5640でJPEGを使用する場合は必ずPSRAMを有効にすること（\texttt{-D BOARD\_HAS\_PSRAM}）。
\end{notebox}

% ---- ServoModule ----
\needspace{5\baselineskip}
\subsubsection{ServoModule — サーボモーター制御}

\begin{apibox}{ServoModule}
\textbf{定義ファイル:} \texttt{lib/ServoModule/ServoModule.h/.cpp}\\
\textbf{外部ライブラリ:} なし（ESP32 LEDCによるPWM直接制御）\\
\textbf{役割:} 出力モジュール — サーボモーターを角度指定で制御する
\end{apibox}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{構造体} & \textbf{メンバ} & \textbf{説明} \\
\midrule
\texttt{ServoConfig} & \texttt{pin} & サーボ信号ピン（GPIO） \\
 & \texttt{pwmChannel} & LEDCチャネル番号（0--15） \\
 & \texttt{minPulseUs} & 最小パルス幅 [$\mu$s]（0度に対応、例: 500） \\
 & \texttt{maxPulseUs} & 最大パルス幅 [$\mu$s]（180度に対応、例: 2500） \\
 & \texttt{defaultAngle} & 初期角度 [度]（0--180） \\
\midrule
\texttt{ServoData} & \texttt{targetAngle} & 目標角度 [度]（0--180、デフォルト: 90） \\
 & \texttt{currentAngle} & 現在の角度 [度]（0--180、デフォルト: 90） \\
\bottomrule
\end{tabularx}

\begin{notebox}[PWM制御の仕組み]
サーボの制御信号は50Hz（周期20ms）のPWMで、パルス幅500--2500$\mu$sが0--180度に対応する。
16bit分解能のLEDCを使用し、角度$\to$パルス幅$\to$デューティ値に変換して出力する。\\
\texttt{update()}は\texttt{targetAngle}が\texttt{currentAngle}と異なる場合のみPWMを更新する。
\end{notebox}

% ---- DriveMotorModule ----
\needspace{5\baselineskip}
\subsubsection{DriveMotorModule — DCモーター単体制御}

\begin{apibox}{DriveMotorModule}
\textbf{定義ファイル:} \texttt{lib/DriveMotorModule/DriveMotorModule.h/.cpp}\\
\textbf{外部ライブラリ:} なし（GPIO + LEDC PWMで直接制御）\\
\textbf{役割:} 出力モジュール — Hブリッジドライバ経由でDCモーターを制御する。
単体で使用する場合は\texttt{drive()}メソッドで直接制御する。
ChassisModule等の統合モジュールの内部部品としても使用される
\end{apibox}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{構造体} & \textbf{メンバ} & \textbf{説明} \\
\midrule
\texttt{DriveMotorConfig} & \texttt{in1Pin / in2Pin} & 方向制御ピン（GPIO） \\
 & \texttt{pwmPin} & PWM速度制御ピン \\
 & \texttt{pwmChannel} & LEDCチャネル番号（0--15） \\
 & \texttt{pwmFreqHz} & PWM周波数 [Hz]（例: 1220） \\
 & \texttt{minPowerThreshold} & 最小出力閾値 [\%]（これ以下は停止扱い） \\
\midrule
\texttt{DriveMotorData} & \texttt{power} & 目標出力 [-100.0 $\sim$ 100.0]（デフォルト: 0.0） \\
\bottomrule
\end{tabularx}

\begin{notebox}[統合モジュールの内部部品としての使用]
\texttt{DriveMotorModule}を\texttt{ChassisModule}等の統合モジュール内部で使用する場合、
\texttt{SystemData}に\texttt{DriveMotorData}を含める必要はない。
統合モジュールが内部で\texttt{drive()}メソッドを直接呼び出すため、
\texttt{update(SystemData\&)}は空実装となる。\\
\texttt{SystemData}への登録・\texttt{outputModules[]}への追加が必要なのは、
モーターを単体で独立制御する場合のみである。
\end{notebox}

\paragraph{方向制御ロジック}
IN1/IN2の組み合わせで正転・逆転・ブレーキを制御する。

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{IN1} & \textbf{IN2} & \textbf{PWM} & \textbf{動作} \\
\midrule
HIGH & LOW & デューティ比 & 正転（power > 0） \\
LOW & HIGH & デューティ比 & 逆転（power < 0） \\
HIGH & HIGH & 最大 & ブレーキ（短絡） \\
LOW & LOW & 0 & 停止（フリー） \\
\bottomrule
\end{tabularx}

\begin{cautionbox}[逆転保護]
正転$\leftrightarrow$逆転の直接切り替え時、1フレーム分のブレーキを自動挿入してモーターを保護する。
停止$\to$正転・停止$\to$逆転は即座に方向転換する。
\end{cautionbox}

\paragraph{drive()メソッド}
\texttt{drive(float power)}は\texttt{SystemData}を経由せずに直接モーターを制御するメソッド。
\texttt{ChassisModule}等の統合モジュールからの内部呼び出しや、
単体使用時のロジックフェーズからの直接制御に使用する。

% ---- ChassisModule ----
\needspace{5\baselineskip}
\subsubsection{ChassisModule — 4輪統合足回り制御}

\begin{apibox}{ChassisModule}
\textbf{定義ファイル:} \texttt{lib/ChassisModule/ChassisModule.h/.cpp}\\
\textbf{外部ライブラリ:} なし\\
\textbf{役割:} 出力モジュール（機能統合系） — 4つのDriveMotorModuleを内部に保持し、
前進・横移動・回転の3軸入力からベクトル合成して各モーターを制御する
\end{apibox}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{構造体} & \textbf{メンバ} & \textbf{説明} \\
\midrule
\texttt{ChassisConfig} & \texttt{motors[4]} & 各モーターのDriveMotorConfig（左前/右前/左後/右後） \\
 & \texttt{wheelPattern} & ホイールパターン（OMNI / MECANUM / FOURWD） \\
 & \texttt{maxOutputPercent} & 最大出力割合 [\%]（例: 90.0） \\
\midrule
\texttt{ChassisData} & \texttt{forwardSpeed} & 前進速度 [-100 $\sim$ 100]（正:前進、負:後退） \\
 & \texttt{lateralSpeed} & 横移動速度 [-100 $\sim$ 100]（正:右、負:左） \\
 & \texttt{rotationSpeed} & 回転速度 [-100 $\sim$ 100]（正:右回転、負:左回転） \\
 & \texttt{motorOutputs[4]} & 各モーター実出力値（デバッグ用、update後に更新） \\
\bottomrule
\end{tabularx}

\paragraph{ホイールパターン（WheelPattern列挙型）}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{パターン} & \textbf{説明} \\
\midrule
\texttt{OMNI} & オムニホイール（全方向移動 + 回転） \\
\texttt{MECANUM} & メカナムホイール（全方向移動 + 回転、ローラー配置が異なる） \\
\texttt{FOURWD} & 4輪駆動（前後 + 回転のみ、横移動不可） \\
\bottomrule
\end{tabularx}

\paragraph{制御フロー}
\texttt{update()}は以下の順序で処理を行う。

\begin{enumerate}
    \item \textbf{ベクトル合成}: ホイールパターンに対応した方向行列と3軸入力から、各モーターの出力値を計算する
    \item \textbf{リミッタ}: 最大絶対値が100を超えた場合、全モーターを等比縮小して飽和を防ぐ
    \item \textbf{スケーリング}: \texttt{maxOutputPercent}で全体の出力割合を調整する
    \item \textbf{駆動}: 各内部\texttt{DriveMotorModule}の\texttt{drive()}を呼び出す
\end{enumerate}

\begin{notebox}[機能統合系モジュールのパターン]
\texttt{ChassisModule}は内部に複数の\texttt{DriveMotorModule}を保持する「機能統合系モジュール」の実装例である。
内部モジュールは\texttt{IModule}として独立管理されるのではなく、親モジュールの\texttt{init()}/\texttt{update()}/\texttt{deinit()}から直接制御される。\\
外部からは1つのモジュール（\texttt{ChassisModule}）として扱い、3軸の速度入力だけで足回り全体を制御できる。
\end{notebox}

\begin{lstlisting}[style=cppstyle, caption={ChassisModuleの使用例（ロジックフェーズ）}]
void applyPattern(SystemData& data) {
    // コントローラー入力を足回りに反映
    data.chassis.forwardSpeed  = controller.getStickY();   // 前後
    data.chassis.lateralSpeed  = controller.getStickX();   // 横移動
    data.chassis.rotationSpeed = controller.getRotation();  // 回転
}
\end{lstlisting}
