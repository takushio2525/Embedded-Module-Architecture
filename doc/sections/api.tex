% API仕様
\section{API仕様}

\subsection{IModule<T>テンプレートインターフェース}

\begin{apibox}{IModule<T> — モジュールテンプレートインターフェース}
\textbf{定義ファイル:} \texttt{lib/ModuleCore/IModule.h}

全ハードウェアモジュールが実装すべき抽象インターフェース。
C++テンプレートにより、プロジェクト固有の型（\texttt{SystemData}等）に一切依存しない。
\texttt{T}にはプロジェクトで定義した共有データ構造体の型を指定する。
\end{apibox}

\begin{lstlisting}[style=cpp, caption={IModule<T>テンプレートインターフェース定義}]
// IModule.h — プロジェクト非依存のコアヘッダー
#pragma once

// <typename T> と書くことで、
// 「Tという型（構造体）を後から指定する」という宣言になる
template <typename T>
class IModule {
public:
    // 仮想デストラクタ
    virtual ~IModule() {}

    // モジュールの初期化
    // 戻り値: true=成功, false=失敗
    virtual bool init() = 0;

    // モジュールの更新（毎ループ呼び出される）
    // 引数: data - プロジェクト共有データへの参照
    virtual void update(T& data) = 0;
};
\end{lstlisting}

\begin{notebox}{テンプレートを使う理由}
テンプレートを使わない場合、\texttt{IModule.h}内で\texttt{SystemData}を\texttt{\#include}する必要がある。
しかし\texttt{SystemData}はプロジェクト固有の型であるため、コアライブラリがプロジェクトに依存してしまう。\\
\\
テンプレートにより、\texttt{IModule.h}は何も\texttt{\#include}せずに済み、
どんなプロジェクトにもそのままコピーして流用できる。
\end{notebox}

\subsubsection{init()}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
戻り値 & \texttt{bool} — \texttt{true}で初期化成功、\texttt{false}で失敗 \\
呼び出しタイミング & \texttt{setup()}内で1回のみ \\
責務 & ハードウェアの初期化、ピン設定、ModuleTimerの\texttt{setTime()}呼び出し等 \\
エラー時 & \texttt{false}を返す。呼び出し側でモジュールの無効化やログ出力を行う \\
\bottomrule
\end{tabularx}

\subsubsection{update(T\& data)}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
引数 & \texttt{T\&} — プロジェクト共有データへの参照（nullにならない） \\
戻り値 & \texttt{void} \\
呼び出しタイミング & \texttt{loop()}内で毎ループ呼び出される \\
責務 & センサー読み取り、出力制御、共有データの更新 \\
エラー時 & 共有データ内のサブ構造体にエラーフラグを設定 \\
\bottomrule
\end{tabularx}

\subsubsection{使用例}

\begin{lstlisting}[style=cpp, caption={モジュール実装の例}]
// LedModule.h
#pragma once
#include "IModule.h"
#include "SystemData.h"  // プロジェクト固有

// モジュール固有のConfig構造体
struct LedConfig {
    uint8_t ledPin;  // LED出力ピン
};

// モジュール固有のデータ構造体
struct LedData {
    bool state;
    uint8_t brightness;
};

// テンプレート引数にSystemDataを指定して継承
class LedModule : public IModule<SystemData> {
private:
    LedConfig _config;  // Configをメンバとして保持
public:
    // コンストラクタでConfig構造体を受け取る
    LedModule(const LedConfig& config) : _config(config) {}

    bool init() override {
        pinMode(_config.ledPin, OUTPUT);  // Configのピンを使用
        return true;
    }

    // data はポインタではなく参照なので、ドット演算子でアクセス
    void update(SystemData& data) override {
        digitalWrite(_config.ledPin, data.led.state);
    }
};
\end{lstlisting}

% -------------------------------------------

\subsection{ModuleTimerクラス}

\begin{apibox}{ModuleTimer — ノンブロッキングタイマー}
\textbf{定義ファイル:} \texttt{lib/ModuleCore/ModuleTimer.h}

\texttt{millis()}ベースのシンプルなタイマークラス。
\texttt{delay()}を使用せずに周期実行・デバウンス・ワンショット・タイムアウト等を実現する。
インスタンスを複数作成することで、モジュール内で複数のタイマーを独立に管理できる。
IModuleと同様にプロジェクト非依存であり、そのまま流用可能。
\end{apibox}

\begin{lstlisting}[style=cpp, caption={ModuleTimerクラス定義}]
// ModuleTimer.h — プロジェクト非依存のコアヘッダー
#pragma once
#include <Arduino.h>

class ModuleTimer {
private:
    unsigned long _startTime;

public:
    // コンストラクタ
    ModuleTimer() : _startTime(0) {}

    // 基準時刻をセット
    // 引数なし: getNowTime()が0から開始
    // 引数あり: getNowTime()が指定値から開始
    void setTime(unsigned long offsetMs = 0) {
        _startTime = millis() - offsetMs;
    }

    // 基準時刻からの経過時間(ms)を取得
    unsigned long getNowTime() const {
        return millis() - _startTime;
    }
};
\end{lstlisting}

\subsubsection{setTime(unsigned long offsetMs = 0)}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
引数 & \texttt{offsetMs}（デフォルト: 0） — 初期オフセット値(ms) \\
戻り値 & \texttt{void} \\
動作 & 内部の基準時刻を\texttt{millis() - offsetMs}に設定する。
       \texttt{offsetMs = 0}の場合、直後の\texttt{getNowTime()}は0を返す。
       \texttt{offsetMs = 100}の場合、直後の\texttt{getNowTime()}は100を返す \\
用途 & タイマーの開始・リセット。\texttt{init()}内および周期リセット時に使用 \\
\bottomrule
\end{tabularx}

\subsubsection{getNowTime() const}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
引数 & なし \\
戻り値 & \texttt{unsigned long} — 最後の\texttt{setTime()}からの経過時間(ms) \\
動作 & \texttt{millis() - \_startTime}を返す \\
用途 & 経過時間の判定。条件分岐と組み合わせて周期実行・デバウンス等を実現 \\
\bottomrule
\end{tabularx}

\subsubsection{使用パターン}

\begin{lstlisting}[style=cpp, caption={周期実行パターン}]
ModuleTimer readTimer;

bool init() override {
    readTimer.setTime();
    return true;
}

void update(SystemData& data) override {
    if (readTimer.getNowTime() >= 1000) {
        readTimer.setTime();  // リセット
        data.temp.temperature = readSensor();
    }
}
\end{lstlisting}

\begin{lstlisting}[style=cpp, caption={デバウンスパターン}]
ModuleTimer debounce;

void update(SystemData& data) override {
    bool rawInput = digitalRead(BUTTON_PIN);

    if (rawInput != lastRawInput) {
        debounce.setTime();  // 入力変化でリセット
        lastRawInput = rawInput;
    }

    if (debounce.getNowTime() >= 50) {
        data.button.pressed = lastRawInput;  // 50ms安定で確定
    }
}
\end{lstlisting}

% -------------------------------------------

\subsection{ProjectConfigファイル}

\begin{apibox}{ProjectConfig — プロジェクト固有設定の集約}
\textbf{定義ファイル:} \texttt{include/ProjectConfig.h}（プロジェクト固有）

各モジュールのConfigインスタンス（ピンアサイン等）と\texttt{SystemData}構造体を
1つのファイルに集約した、プロジェクト固有の設定ファイル。
\texttt{IModule<T>}のテンプレート引数\texttt{T}として指定する\texttt{SystemData}型は、このファイルで定義する。
\end{apibox}

\subsubsection{設計ルール}

\begin{enumerate}
    \item 各モジュールは、自身の\texttt{Config}構造体（型定義）と\texttt{Data}構造体（型定義）をモジュールのヘッダーファイル内に定義する
    \item \texttt{ProjectConfig.h}には、Configの\textbf{インスタンス}（実際のピン番号等の値）を定義する
    \item \texttt{SystemData}構造体は、プロジェクトで使用するモジュールのData構造体をメンバとして持つ
    \item モジュールは\texttt{update()}内で、自身のData構造体に書き込み、必要に応じて他モジュールのData構造体を読み取る
\end{enumerate}

\begin{lstlisting}[style=cpp, caption={ProjectConfig構成例}]
// ProjectConfig.h
#pragma once
#include "LedModule.h"
#include "TempSensorModule.h"
#include "CameraModule.h"

// ===== モジュールConfig（ピンアサイン・設定値） =====
const LedConfig LED_CONFIG = { .ledPin = 13 };
const TempSensorConfig TEMP_CONFIG = { .tempPin = 36 };
const CameraConfig CAMERA_CONFIG = { .sdaPin = 21, .sclPin = 22 };

// ===== システムデータ =====
struct SystemData {
    LedData led;
    TempData temp;
    CameraData camera;
};
\end{lstlisting}

\begin{notebox}{ProjectConfig.hの構成}
\texttt{ProjectConfig.h}は2つのセクションで構成される：\\
\textbf{モジュールConfigセクション} — 各モジュールのConfigインスタンス（ピン番号等の具体的な値）。
回路設計担当者はこのセクションだけを参照すればよい。\\
\textbf{システムデータセクション} — 各モジュールのData構造体を集約した\texttt{SystemData}構造体。
モジュール間のデータ連携に使用する。
\end{notebox}

% -------------------------------------------

\subsection{モジュールConfig構造体}

\begin{apibox}{モジュールConfig — ハードウェア設定構造体}
各モジュールが必要とするピンアサインや設定をまとめた構造体。
各モジュールのヘッダーファイル内で型を定義し、
\texttt{ProjectConfig.h}内で具体的な値を持つインスタンスを定義する。
\end{apibox}

\subsubsection{命名規則}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{要素} & \textbf{命名規則} & \textbf{例} \\
\midrule
Config構造体の型名 & \texttt{\{Module\}Config} & \texttt{LedConfig}, \texttt{CameraConfig} \\
Configインスタンス名 & \texttt{\{MODULE\}\_CONFIG}(大文字) & \texttt{LED\_CONFIG}, \texttt{CAMERA\_CONFIG} \\
Data構造体の型名 & \texttt{\{Module\}Data} & \texttt{LedData}, \texttt{CameraData} \\
\bottomrule
\end{tabularx}

\subsubsection{設計ルール}

\begin{enumerate}
    \item Config構造体の\textbf{型定義}は、各モジュールのヘッダーファイル内に記述する
    \item Config構造体の\textbf{インスタンス}（実際のピン番号等）は\texttt{ProjectConfig.h}に集約する
    \item モジュールのコンストラクタは\texttt{const \{Module\}Config\&}（const参照）でConfigを受け取る
    \item 受け取ったConfigは、メンバ変数\texttt{\_config}としてコピー保持する
    \item ピン番号はハードコードせず、必ず\texttt{\_config}経由でアクセスする
\end{enumerate}

\begin{lstlisting}[style=cpp, caption={Config構造体の定義と使用例}]
// TempSensorModule.h
#pragma once
#include "IModule.h"
#include "ModuleTimer.h"

// --- Config構造体（ピンアサイン・設定） ---
struct TempSensorConfig {
    uint8_t tempPin;          // アナログ入力ピン
    unsigned long interval;   // 読み取り間隔(ms)
};

// --- Data構造体（モジュール間共有データ） ---
struct TempData {
    float temperature;
    bool isValid;
};

class TempSensorModule : public IModule<SystemData> {
private:
    TempSensorConfig _config;
    ModuleTimer _readTimer;
public:
    // コンストラクタ: Config構造体をconst参照で受け取る
    TempSensorModule(const TempSensorConfig& config)
        : _config(config) {}

    bool init() override {
        pinMode(_config.tempPin, INPUT);
        _readTimer.setTime();
        return true;
    }

    void update(SystemData& data) override {
        if (_readTimer.getNowTime() >= _config.interval) {
            _readTimer.setTime();
            data.temp.temperature = analogRead(_config.tempPin);
            data.temp.isValid = true;
        }
    }
};
\end{lstlisting}
