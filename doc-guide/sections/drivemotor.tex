% DriveMotorModule
\section{【応用】DriveMotorModule — PWM 2ピン制御 + 統合部品}

\begin{patternbox}{DriveMotorModule で学べるパターン}
\begin{itemize}
    \item Hブリッジによる方向制御（IN1/IN2 + PWM）
    \item 逆転保護（ブレーキ自動挿入）
    \item drive()メソッドによるSystemData非経由の直接制御
    \item 統合モジュールの「内部部品」としての使い方
\end{itemize}
\end{patternbox}

\subsection{2つの使い方}

\texttt{DriveMotorModule}は2つのモードで使える。

\begin{enumerate}
    \item \textbf{単体使用} — \texttt{outputModules[]}に登録し、\texttt{SystemData}経由で制御
    \item \textbf{統合モジュールの内部部品} — \texttt{ChassisModule}等が内部で\texttt{drive()}を呼ぶ
\end{enumerate}

\begin{lstlisting}[style=cppstyle, caption={DriveMotorModule.h — drive()メソッド}]
class DriveMotorModule : public IModule {
public:
    // SystemData経由の制御（単体使用時）
    void updateOutput(SystemData& data) override;

    // SystemData非経由の直接制御（統合モジュールから使用）
    void drive(float power);
    void brake();
};
\end{lstlisting}

\begin{pointbox}[updateOutput()とdrive()の使い分け]
\texttt{updateOutput()}はSystemDataの\texttt{DriveMotorData.power}を読んで\texttt{drive()}を呼ぶラッパー。
統合モジュールの内部部品として使う場合は\texttt{drive()}を直接呼び、
\texttt{updateOutput()}は空実装（呼ばれない）となる。
この設計により、同じモジュールコードを単体でも統合でも使い回せる。
\end{pointbox}

\subsection{逆転保護}

\begin{lstlisting}[style=cppstyle, caption={DriveMotorModule.cpp — 逆転保護}]
void DriveMotorModule::drive(float power) {
    Direction newDir = (power > 0) ? FORWARD
                     : (power < 0) ? REVERSE : STOP;

    // 正転<->逆転の直接切り替え時、1フレーム分のブレーキを挿入
    if ((_lastDir == FORWARD && newDir == REVERSE) ||
        (_lastDir == REVERSE && newDir == FORWARD)) {
        brake();
        _lastDir = STOP;
        return;  // 次のupdateOutput()で実際の方向転換が行われる
    }
    // ... 実際のPWM制御 ...
}
\end{lstlisting}

\begin{pointbox}[1フレーム遅延によるモーター保護]
正転→逆転の直接切り替えはモーターやドライバICに大きな負荷がかかる。
1フレーム分（1ループ分）のブレーキを挿入することで、物理的に安全な方向転換を実現する。
3フェーズモデルの「毎ループ1回だけ呼ばれる」性質を利用した保護パターン。
\end{pointbox}

% ─── API仕様 ───────────────────────────────
\subsection{API仕様}


\begin{apibox}{DriveMotorModule --- DCモーター単体制御モジュール}
\textbf{定義ファイル:} \texttt{lib/DriveMotorModule/DriveMotorModule.h / .cpp}\\
\textbf{分類:} 出力モジュール（\texttt{updateOutput()}のみオーバーライド）\\
\textbf{対象デバイス:} DCモーター（Hブリッジドライバ経由）

IN1/IN2で方向制御、PWMで速度制御を行うHブリッジドライバ用モジュール。
ChassisModule等の統合モジュールが内部で保持し、\texttt{drive()}メソッドで直接制御する設計。
\end{apibox}

\begin{cautionbox}[\texttt{outputModules[]}に登録しない]
DriveMotorModuleは\textbf{ChassisModule等の統合モジュールが内部で保持する}前提の設計である。
\texttt{outputModules[]}には登録せず、ChassisModuleの\texttt{updateOutput()}経由で\texttt{drive()}が呼ばれる。
\texttt{updateOutput()}は空実装であり、直接呼び出しても何も起きない。
\end{cautionbox}

\subsubsection{機能概要}

\begin{itemize}
    \item IN1/IN2 + PWMによるHブリッジ制御（正転・逆転・停止）
    \item 出力値 $-100$\textasciitilde$100$\%の範囲指定
    \item 最小出力閾値（\texttt{minPowerThreshold}）以下で停止扱い
    \item 方向反転時の1フレームブレーキ挿入（モーター保護）
    \item 外部直接制御用の\texttt{drive()}メソッド
\end{itemize}

% --- Config ---
\subsubsection{Config構造体}

\begin{funcblock}{DriveMotorConfig}
Hブリッジドライバの接続ピンとPWMパラメータを定義する。

\begin{longtable}{l l l p{4.2cm}}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{制約・既定値} & \textbf{説明} \\
\midrule
\endhead
\texttt{in1Pin}            & \texttt{uint8\_t}  & ---       & 方向制御ピン1 \\
\texttt{in2Pin}            & \texttt{uint8\_t}  & ---       & 方向制御ピン2 \\
\texttt{pwmPin}            & \texttt{uint8\_t}  & ---       & PWM速度制御ピン \\
\texttt{pwmChannel}        & \texttt{uint8\_t}  & 0--15     & LEDCチャネル番号 \\
\texttt{pwmFreqHz}         & \texttt{uint32\_t} & 例: 1220  & PWM周波数 [Hz] \\
\texttt{minPowerThreshold} & \texttt{float}     & 例: 5.0   & 最小出力閾値 [\%]。この値以下の出力は停止扱い \\
\bottomrule
\end{longtable}
\end{funcblock}

% --- Data ---
\subsubsection{Data構造体}

\begin{funcblock}{DriveMotorData}
モーターの目標出力を保持する。

\begin{longtable}{l l l p{5.0cm}}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{初期値} & \textbf{説明} \\
\midrule
\endhead
\texttt{power} & \texttt{float} & \texttt{0.0f} & 目標出力 [$-100.0$\textasciitilde$100.0$]。正:正転、負:逆転、0:停止 \\
\bottomrule
\end{longtable}
\end{funcblock}

% --- メソッド ---
\subsubsection{メソッド}

\begin{funcblock}{DriveMotorModule(const DriveMotorConfig\& config)}
\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
引数   & \texttt{config} --- \texttt{DriveMotorConfig}へのconst参照 \\
動作   & Configを\texttt{\_config}にコピー保持する \\
\bottomrule
\end{tabularx}
\end{funcblock}

\begin{funcblock}{bool init()}
\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
戻り値   & \texttt{bool} --- 常に\texttt{true} \\
動作     & IN1/IN2ピンを\texttt{OUTPUT}に設定。PWMピンのLEDCチャネルを初期化。モーターを停止状態にする \\
\bottomrule
\end{tabularx}
\end{funcblock}

\begin{funcblock}{void updateOutput(SystemData\& data)}
\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
動作   & 空実装。ChassisModuleが\texttt{drive()}を直接呼ぶため、このメソッドは使用しない \\
\bottomrule
\end{tabularx}
\end{funcblock}

\begin{funcblock}{void drive(float power)}
\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
引数       & \texttt{power} --- 出力値 [$-100.0$\textasciitilde$100.0$] \\
動作       & 出力値の絶対値が\texttt{minPowerThreshold}以下なら停止。方向が前回と反転した場合は1フレームブレーキを挿入。IN1/IN2で方向設定、PWMデューティで速度制御 \\
呼び出し元 & ChassisModuleの\texttt{updateOutput()}内 \\
\bottomrule
\end{tabularx}
\end{funcblock}

\begin{notebox}[方向反転時のブレーキ]
前回の方向と今回の方向が逆転した場合（正転→逆転、逆転→正転）、
モーターと駆動回路を保護するため1フレーム分のブレーキ（IN1=IN2=LOW、PWM=0）を挿入する。
次のループで新しい方向の駆動が開始される。
\end{notebox}

% --- 使用例 ---
\subsubsection{使用例}

\begin{lstlisting}[style=cppstyle, caption={DriveMotorModuleの使用例（ChassisModule内部）}]
// ChassisConfigのmotors配列で定義
const ChassisConfig CHASSIS_CONFIG = {
    .motors = {
        // [0] 左前
        {.in1Pin = 4,  .in2Pin = 16, .pwmPin = 17,
         .pwmChannel = 2, .pwmFreqHz = 1220,
         .minPowerThreshold = 5.0f},
        // ... (右前、左後、右後)
    },
    .wheelPattern     = WheelPattern::OMNI,
    .maxOutputPercent = 90.0f,
};
// DriveMotorModuleはChassisModuleが内部で生成・管理する
\end{lstlisting}
